# 📝 สรุป: วิธี Deploy เว็บไซต์

## 🎯 ชื่อวิธีนี้คือ: **"GitHub Actions Auto Deploy"**

หรือเรียกเต็มๆ ว่า: **"Continuous Deployment (CD) with GitHub Actions"**

---

## 💡 หลักการทำงาน

```
คุณ push code → GitHub Actions ทำงาน → Build & Deploy อัตโนมัติ
```

ไม่ต้องรัน command deploy เองเลย!

---

## 🚀 คำสั่งที่ใช้ (จำไว้!)

### ✅ **คำสั่งหลัก (ใช้บ่อย):**

```bash
# 1. แก้ไขโค้ด
# 2. เซฟไฟล์
# 3. รันคำสั่งนี้:

git add .
git commit -m "อธิบายว่าแก้อะไร"
git push
```

**เท่านี้พอ!** เว็บจะอัปเดตเองใน 2-3 นาที

---

## 📋 คำสั่งครั้งแรก (Setup เฉพาะครั้งเดียว)

```bash
# ครั้งแรกเท่านั้น - สร้าง repository
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

---

## 🔧 ไฟล์สำคัญที่ต้องมี

1. **`.github/workflows/deploy.yml`** ← GitHub Actions config
2. **`next.config.ts`** ← ต้องมี:

   ```typescript
   output: 'export'
   basePath: '/ชื่อrepo'
   ```

---

## 📱 ตรวจสอบสถานะ

### ดู Deploy Status

```
https://github.com/USERNAME/REPO_NAME/actions
```

### เข้าเว็บไซต์

```
https://USERNAME.github.io/REPO_NAME/
```

---

## ❌ ไม่ต้องรันอีกแล้ว

- ❌ `npm run deploy`
- ❌ `npx gh-pages -d out`
- ❌ `vercel deploy`

**แค่ `git push` พอ!**

---

## 📊 เปรียบเทียบวิธีต่างๆ

| วิธี | คำสั่งหลัก | Auto? |
|------|-----------|-------|
| **GitHub Actions** ✅ | `git push` | ✅ ใช่ |
| gh-pages package | `npm run deploy` | ❌ ไม่ |
| Vercel CLI | `vercel --prod` | ❌ ไม่ |
| Netlify Drop | ลาก folder | ❌ ไม่ |

---

## 🎓 คำศัพท์ที่ควรรู้

- **GitHub Actions** = ระบบรัน command อัตโนมัติบน GitHub
- **Workflow** = ขั้นตอนการทำงานที่กำหนดไว้
- **CI/CD** = Continuous Integration / Continuous Deployment
- **Static Site** = เว็บไซต์ที่เป็นไฟล์ HTML/CSS/JS ล้วนๆ
- **GitHub Pages** = บริการ hosting เว็บไซต์ฟรีของ GitHub

---

## 📝 ตัวอย่างการใช้งานจริง

### วันนี้แก้ไข Customer Modal

```bash
# 1. แก้โค้ดใน CustomerModal.tsx
# 2. เซฟไฟล์
# 3. รัน:
git add .
git commit -m "แก้ไข Customer Modal ให้มี View/Edit mode"
git push
# 4. รอ 2-3 นาที
# 5. เว็บอัปเดตแล้ว! ✅
```

### พรุ่งนี้เพิ่มหน้าใหม่

```bash
# 1. สร้างหน้าใหม่
# 2. เซฟไฟล์
# 3. รัน:
git add .
git commit -m "เพิ่มหน้า Report"
git push
# เสร็จ! ✅
```

---

## 🎯 สรุป 3 ประโยค

1. **วิธีนี้ชื่อ:** GitHub Actions Auto Deploy
2. **คำสั่งที่ใช้:** `git add .` → `git commit -m "..."` → `git push`
3. **ทำงาน:** อัตโนมัติ 100% ไม่ต้องทำอะไรเพิ่ม

---

## 💾 บันทึกไว้ว่า

ถ้าในอนาคต AI agent อื่นถามว่า **"Deploy ยังไง?"**

บอกว่า: **"ใช้ GitHub Actions แค่ git push ก็ deploy เองอัตโนมัติ"**

ไฟล์สำคัญ: `.github/workflows/deploy.yml`

---

เท่านี้ครับ! 🚀 จำง่ายๆ ว่า **"แค่ push ก็ deploy เอง"**
